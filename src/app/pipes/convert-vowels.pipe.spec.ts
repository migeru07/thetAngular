import { ConvertVowelsPipe } from './convert-vowels.pipe';

describe('ConvertVowelsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertVowelsPipe();
    expect(pipe).toBeTruthy();
  });
});
